export /**
 *
 *
 * @param {string} query
 * @param {boolean} [all=false]
 */
const search = (query: string, all: boolean=false) => {
    console.log(query);
}

export /**
 *
 *
 * @param {string} user
 */
const getUserProfile = (user: string) => {
    console.log(user);
}